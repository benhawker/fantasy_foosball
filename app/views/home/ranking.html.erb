	<div class="container">
		<% if @teams.any? %>
	 		<h2 class="text-center">Team Leaderboard</h2> 
		  <table class="table">
		    <thead>
		      <tr>
		        <th>Player One</th>
		        <th>Player Two</th>
		        <th>Total Wins</th>
		        <th>Options</th>
		      </tr>
		    </thead>
		    <tbody>
		      <% @teams.each do |team| %>
			      <tr>
			        <td><%= team.player_one %></td>
			        <td><%= team.player_two %></td>
			        <td>*</td>
			        <td><%= link_to "Delete Team", team_path(team), method: :delete %></td>
			      </tr>
		     	<% end %>
		    </tbody>
		  </table>
		<% else %>
			<p>No teams yet.<p>
		<% end %>
	</div>