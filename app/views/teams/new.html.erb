<div class="col-sm-6 col-centered">
  <h2 class="text-center">Create your new team below</h2>
	<%= form_for [@team], :html => {:multipart => true} do |f| %>
		<%= f.label :player_one %>
		<%= f.select :player_one, User.all.map(&:first_name), class: "form-control" %>
		<br>
		<%= f.label :player_two %>
		<%= f.select :player_two, User.all.map(&:first_name), class: "form-control" %>
	  <%= f.submit "Create Team!", class: "form-control" %>
	<% end %>
</div>

